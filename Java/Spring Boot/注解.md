## @Autowired、@Qualifier、@Resource

    Spring

    @Autowired(required)    
        byType注入
    @Qualifier
        byName注入

    J2EE
        javax.annotation.Resouce

    @Resource(name, type)
        byName（默认）